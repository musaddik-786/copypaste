Yes — this is exactly what you should do. Since multiple MCPs will use Phoenix, the common code should live at the MCP level, not inside one MCP module.

You are thinking in the correct modular architecture direction (this is how real AI agent systems are structured).

⸻

✔️ Correct structure for your Jarvis project

Instead of:

attachment_summary_mcp/
    prompt_loader.py   ❌ (too local)

Move to:

Jarvis/
│
├── MCP/
│     ├── common/              ← CREATE THIS
│     │     ├── phoenix_prompt.py
│     │     ├── phoenix_tracing.py (optional later)
│     │
│     ├── attachment_summary_mcp/
│     │     ├── newsummary.py
│     │     ├── summary.py
│     │
│     ├── risk_mcp/
│     ├── extraction_mcp/

This becomes your shared MCP utilities layer.

⸻

✔️ Step 1 — Create common Phoenix loader

Create:

Jarvis/MCP/common/phoenix_prompt.py

phoenix_prompt.py

from phoenix.client import Client
import os
from functools import lru_cache


PHOENIX_API_KEY = os.getenv("PHOENIX_API_KEY")

PHOENIX_ENDPOINT = os.getenv("PHOENIX_ENDPOINT")


phoenix_client = Client(

    base_url=PHOENIX_ENDPOINT,

    api_key=PHOENIX_API_KEY

)


@lru_cache
def get_prompt_template(prompt_version_id:str):

    prompt_stored = phoenix_client.prompts.get(

        prompt_version_id=prompt_version_id

    )

    prompt_messages = prompt_stored._template["messages"]

    system_prompt = next(

        (

            item["content"][0]["text"]

            for item in prompt_messages

            if item.get("role") == "system"

        ),

        None

    )

    if not system_prompt:

        raise ValueError(
            "Phoenix prompt missing"
        )

    return system_prompt



def build_prompt(

    prompt_version_id:str,

    **kwargs

)->str:

    template = get_prompt_template(

        prompt_version_id

    )

    return template.format(

        **kwargs

    )


⸻

✔️ Step 2 — Use in any MCP

Example newsummary.py:

from MCP.common.phoenix_prompt import build_prompt

Then:

prompt = build_prompt(

    "UHJvbXB0VmVyc2lvbjoxNQ==",

    attachments_text=attachments_text,

    email_bodies_text=email_bodies_text

)


⸻

✔️ Step 3 — Use in summary.py

from MCP.common.phoenix_prompt import build_prompt


prompt = build_prompt(

    "UHJvbXB0VmVyc2lvbjoxNg==",

    attachments_text=attachments_text

)


⸻

✔️ Why this is the correct architecture

You now have:

Phoenix logic → ONE place
MCP modules → reuse

Instead of:

Phoenix logic → duplicated everywhere


⸻

✔️ Benefits (important)

Benefit	Why it matters
No duplication	Less bugs
Central change	Change once
Prompt caching	Faster
Clean MCP design	Matches agent architecture
Easier debugging	One location


⸻

✔️ This matches your Omnicore philosophy

You are basically creating:

Common Services Layer

Like:

Mera Brain → shared services
Role agents → use services
Process agents → use services

Phoenix prompt loader becomes a shared MCP service.

⸻

✔️ One more thing professionals also do (optional but recommended)

Usually they also add:

MCP/common/
    phoenix_prompt.py
    phoenix_tracing.py
    llm_client.py
    blob_utils.py

So MCP becomes self-contained.

⸻

✔️ Final verdict

Yes — best place is:

Jarvis/MCP/common/

This is exactly how enterprise AI agent repos are structured.

⸻

If you want the perfect Jarvis structure

I can show you the exact MCP structure used in production AI underwriting agents (it will make your project look like a senior engineer designed it).


