This is my agent docker file

FROM python:3.11.5-bookworm

WORKDIR /EmailIntentAgent

COPY requirements.txt .

RUN pip3 install --no-cache-dir -r requirements.txt

COPY . .


EXPOSE 8098

CMD ["python3","server.py"]


below is my mcp docker file


FROM python:3.11.5-bookworm

# System dependencies required by weasyprint
RUN apt-get update && apt-get install -y \
    libpango-1.0-0 \
    libpangoft2-1.0-0 \
    libgdk-pixbuf2.0-0 \
    libffi-dev \
    shared-mime-info \
    libcairo2 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /MCP

COPY requirements.txt .
RUN pip3 install --no-cache-dir -r requirements.txt

COPY . .

RUN mkdir -p /MCP/data /MCP/credentials

EXPOSE 5425

CMD ["python3","test_main.py"]



tis is my dockerignore
.git
rohan_env
venv
__pycache__/
venv1
env2
token.pickle
